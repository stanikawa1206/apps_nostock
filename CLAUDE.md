## 実行時の安全ルール(bypassPermissionsモード運用のため)
- 本番のeBay APIを呼ぶ操作(出品・編集・削除・メッセージ送信など)は、
  ユーザーから明示的な指示がない限り実行しない
- SQL ServerへのUPDATE/DELETE/DROPなど、データを変更するSQLを実行する前は、
  必ず対象件数と内容を先に報告してから実行する
- rm/Remove-Item など削除系コマンドで、ワイルドカードや広範囲のパスを
  対象にする場合は、実行前に対象パスを一覧表示して確認を取る
- 新しい常駐スクリプトやcronジョブ的なものを追加する際は、
  必ず追加前にユーザーに報告する
